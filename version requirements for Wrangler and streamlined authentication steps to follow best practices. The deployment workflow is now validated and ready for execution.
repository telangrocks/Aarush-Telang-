[1mdiff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml[m
[1mindex 03cd61b..3f6f31d 100644[m
[1m--- a/.github/workflows/deploy.yml[m
[1m+++ b/.github/workflows/deploy.yml[m
[36m@@ -6,7 +6,7 @@[m [mon:[m
   workflow_dispatch:[m
 [m
 env:[m
[31m-  NODE_VERSION: '20'[m
[32m+[m[32m  NODE_VERSION: '22'[m
 [m
 jobs:[m
   # ============================================================================[m
[36m@@ -21,17 +21,15 @@[m [mjobs:[m
     steps:[m
       - name: Checkout code[m
         uses: actions/checkout@v4[m
[31m-[m
[31m-      - name: Verify secrets are configured[m
[32m+[m[32m      - name: Determine Environment and Verify Secrets[m
[32m+[m[32m        id: target # This ID is referenced by the job's outputs[m
         env:[m
           WRANGLER_API_TOKEN: ${{ secrets.WRANGLER_API_TOKEN }}[m
         run: |[m
           if [ "${{ github.ref }}" = "refs/heads/main" ]; then[m
             echo "environment=production" >> $GITHUB_OUTPUT[m
[31m-            echo "🟢 Deploying to PRODUCTION"[m
           else[m
             echo "environment=staging" >> $GITHUB_OUTPUT[m
[31m-            echo "🟡 Deploying to STAGING"[m
           fi[m
           if [ -z "$WRANGLER_API_TOKEN" ]; then[m
             echo "❌ Error: WRANGLER_API_TOKEN secret is not configured."[m
[36m@@ -39,12 +37,6 @@[m [mjobs:[m
           fi[m
           echo "✅ All required secrets are configured."[m
 [m
[31m-      - name: Determine deployment target[m
[31m-        id: target[m
[31m-        run: |[m
[31m-          # This step is now part of the previous step's script block[m
[31m-          # to consolidate logic. It can be removed or kept for clarity.[m
[31m-[m
   # ============================================================================[m
   # DEPLOY BACKEND TO CLOUDFLARE WORKERS[m
   # ============================================================================[m
[36m@@ -76,20 +68,21 @@[m [mjobs:[m
       - name: Verify Wrangler installation[m
         run: wrangler --version[m
 [m
[31m-      - name: Configure Wrangler authentication[m
[31m-        env:[m
[31m-          WRANGLER_API_TOKEN: ${{ secrets.WRANGLER_API_TOKEN }}[m
[31m-        run: echo "Using Wrangler API token"[m
[31m-[m
       - name: Build backend project[m
         working-directory: backend[m
         run: npm run build[m
 [m
[32m+[m[32m      - name: Post-deployment smoke tests[m
[32m+[m[32m        working-directory: backend[m
[32m+[m[32m        run: |[m
[32m+[m[32m          echo "Running post-deployment health checks..."[m
[32m+[m[32m          npm run test:smoke[m
[32m+[m
       - name: Deploy to Cloudflare Workers (Staging)[m
         if: github.ref == 'refs/heads/staging'[m
         working-directory: backend[m
         env:[m
[31m-          WRANGLER_API_TOKEN: ${{ secrets.WRANGLER_API_TOKEN }}[m
[32m+[m[32m          CLOUDFLARE_API_TOKEN: ${{ secrets.WRANGLER_API_TOKEN }}[m
           NODE_ENV: staging[m
         run: |[m
           {[m
[36m@@ -103,7 +96,7 @@[m [mjobs:[m
         if: github.ref == 'refs/heads/main'[m
         working-directory: backend[m
         env:[m
[31m-          WRANGLER_API_TOKEN: ${{ secrets.WRANGLER_API_TOKEN }}[m
[32m+[m[32m          CLOUDFLARE_API_TOKEN: ${{ secrets.WRANGLER_API_TOKEN }}[m
           NODE_ENV: production[m
         run: |[m
           {[m
[36m@@ -113,12 +106,6 @@[m [mjobs:[m
             wrangler deploy --env production[m
           } 2>&1 | tee backend/deployment.log[m
 [m
[31m-      - name: Post-deployment smoke tests[m
[31m-        working-directory: backend[m
[31m-        run: |[m
[31m-          echo "Running post-deployment health checks..."[m
[31m-          npm run test:smoke[m
[31m-[m
       - name: Upload deployment logs[m
         if: always()[m
         uses: actions/upload-artifact@v4[m
[1mdiff --git a/.github/workflows/security.yml b/.github/workflows/security.yml[m
[1mindex ba87b69..910c3be 100644[m
[1m--- a/.github/workflows/security.yml[m
[1m+++ b/.github/workflows/security.yml[m
[36m@@ -80,62 +80,6 @@[m [mjobs:[m
     strategy:[m
       fail-fast: false[m
       matrix:[m
[31m-# Repository Migration & Canonical Project Setup[m
[31m-[m
[31m-## Objective[m
[31m-[m
[31m-I want to transition this Android project so that the **GitHub repository becomes the canonical (single source of truth)**.[m
[31m-[m
[31m-From this point onward, all future development should be based on the repository. My local machine will only be used later to clone the repository, open it in Android Studio, build, debug, and test the application.[m
[31m-[m
[31m-## Tasks[m
[31m-[m
[31m-1. Perform a complete comparison between the local Android project and the repository workspace.[m
[31m-2. Identify every required source file, resource, Gradle file, manifest, asset, workflow, configuration, and dependency needed for a complete Android Studio project.[m
[31m-3. Copy or synchronize any missing project files from the local project into the repository workspace.[m
[31m-4. Preserve the existing directory structure.[m
[31m-5. Ensure that no required project files are omitted.[m
[31m-6. Exclude all machine-specific or generated content, including:[m
[31m-[m
[31m-   * `local.properties`[m
[31m-   * `.gradle/`[m
[31m-   * `build/`[m
[31m-   * IDE caches[m
[31m-   * temporary files[m
[31m-   * signing keystores[m
[31m-   * secrets, tokens, credentials, or environment-specific files[m
[31m-7. Update any project configuration necessary so the repository workspace is internally consistent.[m
[31m-8. Validate that a fresh clone of this repository would contain everything required to open the project successfully in Android Studio after the normal machine-specific setup.[m
[31m-[m
[31m-## Final Audit[m
[31m-[m
[31m-Perform a complete verification and confirm:[m
[31m-[m
[31m-* All Android source code is present.[m
[31m-* All resources are present.[m
[31m-* All Gradle configuration files are present.[m
[31m-* All manifests are present.[m
[31m-* All required assets are present.[m
[31m-* All GitHub workflows are present.[m
[31m-* All required project configuration files are present.[m
[31m-* There are no missing files required for a successful build.[m
[31m-* The repository workspace is complete and ready to become the project's canonical source.[m
[31m-[m
[31m-## Important[m
[31m-[m
[31m-* **Do not perform any Git operations** (no commit, push, pull, merge, rebase, branch creation, or pull request).[m
[31m-* Only prepare the repository workspace.[m
[31m-* Leave all changes uncommitted.[m
[31m-[m
[31m-## Deliverables[m
[31m-[m
[31m-When finished, provide:[m
[31m-[m
[31m-1. A complete list of files that were migrated.[m
[31m-2. A list of files intentionally excluded and the reason.[m
[31m-3. Any remaining issues.[m
[31m-4. Any recommendations before the first commit.[m
[31m-5. A final confirmation stating whether the repository workspace is ready to become the canonical project and can later be cloned into Android Studio for building and testing.[m
         working-directory: ['backend'][m
     [m
     steps:[m
